---
apiVersion: apiextensions.rapyuta.io/v1
kind: Secret
metadata:
  labels:
    app: test
  name: invalid-secret-manifest
spec:
  # This is invalid for a Secret manifest - it's a StaticRoute
  # This should cause pydantic validation errors when processed as a Secret
  routes:
  - name: test-route
    url: https://example.com
