---
apiVersion: apiextensions.rapyuta.io/v1
kind: Package
metadata:
  name: package-wrong-type
  version: v1.0.0
  labels:
    app: test
spec:
  # This is intentionally wrong - it's a Secret manifest but should be Package
  docker:
    email: test@rapyuta.io
    password: testpass
    registry: https://ghcr.io
    username: testuser
  type: Docker
