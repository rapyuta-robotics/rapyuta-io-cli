apiVersion: "apiextensions.rapyuta.io/v1"
kind: "Device"
metadata:
  name: "test-db-device"
  labels:
    app: test-database
    purpose: testing
spec:
  rosDistro: "noetic"
  virtual:
    enabled: True
    arch: "amd64"
    os: "ubuntu"
    codename: "focal"
    wait: True
    expireAfter: '2h'
  docker:
    enabled: True
