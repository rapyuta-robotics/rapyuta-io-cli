apiVersion: api.rapyuta.io/v2
kind: Project
metadata:
  name: test-project-name
  organizationGUID: org-guid
spec:
  users:
    - emailID: "user@example.com"
  userGroups:
    - name: dev-group
  features:
    vpn: true
    tracing: false
